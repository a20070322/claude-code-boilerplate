# Claude Code 最佳实践示例

这是一个完整的 Claude Code 工程化实践示例,基于博客文章 [《25% → 90%！别让 Skills 吃灰: Hooks + Commands + Agents 协同激活 AI 全部能力》](https://blog.csdn.net/leoisaking/article/details/156203326) 实现。

## 🎯 核心目标

通过 **4 个生命周期钩子 + 26 个专业技能 + 斜杠命令 + 自定义代理**,将 AI 技能激活率从 **25% 提升到 90%+**,彻底解决 AI "选择性失忆"问题。

## 📁 项目结构

```
.
├── .claude/                      # Claude Code 配置目录
│   ├── settings.json             # 核心配置 (定义钩子、权限)
│   ├── CLAUDE.md                 # 项目总规范
│   ├── hooks/                    # 生命周期钩子
│   │   ├── session-start.js      # 会话启动时显示项目状态
│   │   ├── skill-forced-eval.js  # 强制技能评估 (核心)
│   │   ├── pre-tool-use.js       # 执行命令前安全检查
│   │   └── stop.js               # 任务完成后总结建议
│   ├── skills/                   # 专业技能
│   │   ├── crud-development/     # CRUD开发规范
│   │   ├── api-development/      # API设计规范
│   │   ├── database-ops/         # 数据库操作规范
│   │   └── code-patterns/        # 代码规范
│   ├── commands/                 # 斜杠命令
│   │   ├── dev.md                # 7步智能开发流程
│   │   ├── crud.md               # 一键生成CRUD代码
│   │   └── check.md              # 代码规范检查
│   └── agents/                   # 自定义代理
│       ├── code-reviewer/        # 代码审查代理
│       └── project-manager/      # 项目管理代理
├── docs/                         # 项目文档
│   ├── 项目状态.md               # 开发进度跟踪
│   └── 待办清单.md               # 待办事项管理
└── README.md                     # 本文件
```

## 🚀 快速开始

### 1. 确认配置文件

确保 `.claude/settings.json` 已正确配置:

```json
{
  "hooks": {
    "SessionStart": [...],
    "UserPromptSubmit": [...],
    "PreToolUse": [...],
    "Stop": [...]
  }
}
```

### 2. 启动 Claude Code

在项目目录下启动 Claude Code,会话启动时会自动触发 `session-start` 钩子,显示:
- 当前 Git 分支
- 未提交变更
- 待办事项
- 快捷命令菜单

### 3. 使用快捷命令

#### /dev - 智能开发
```
/dev 开发用户管理功能
```
自动执行7步开发流程:
1. 需求澄清
2. 技术设计
3. 数据库设计
4. 后端开发
5. 前端开发
6. 测试验证
7. 文档更新

#### /crud - 快速生成代码
```
crud bus_coupon
```
一键生成完整CRUD代码:
- Entity, BO, VO
- DAO, Service, Controller
- Mapper
- 前端 API 和页面

#### /check - 代码检查
```
check
```
全栈代码规范检查:
- 后端代码规范
- 前端代码规范
- 安全问题
- 性能问题

### 4. 使用代理

#### @code-reviewer - 代码审查
```
@code-reviewer
```
自动审查代码质量,生成详细报告。

#### @project-manager - 项目管理
```
@project-manager
```
自动更新项目状态和待办清单。

## 🔑 核心特性

### 1. 强制技能评估

每次用户提问时,`skill-forced-eval` 钩子会强制 AI 评估需要激活哪些技能:

```
用户: 帮我开发一个优惠券管理功能

AI 评估结果:
- crud-development: 是 - 涉及业务模块CRUD开发
- database-ops: 是 - 需要建表和字典配置
- ui-pc: 否 - 用户未要求前端页面

激活技能:
> Skill(crud-development)
> Skill(database-ops)
```

### 2. 安全防护

`pre-tool-use` 钩子会在执行危险命令前拦截:

- ❌ `rm -rf /` - 递归删除根目录
- ❌ `DROP DATABASE` - 删除数据库
- ❌ `FORMAT C:` - 格式化磁盘

### 3. 自动总结

任务完成后,`stop` 钩子会:
- 播放提示音
- 分析代码变更
- 推荐下一步操作
- 清理临时文件

## 📊 效果数据

经过两个月的实际使用统计:

| 指标 | 优化前 | 优化后 | 提升 |
|------|--------|--------|------|
| **技能激活率** | ~25% | 90%+ | **+260%** |
| **CRUD模块开发** | 2-4小时 | 5-10分钟 | **-95%** |
| **代码规范问题** | 5-10个/模块 | 0-2个/模块 | **-80%** |
| **危险操作事故** | 偶发 | 0 | **-100%** |
| **新人上手周期** | 1-2周 | 2-3天 | **-75%** |

## 📖 使用示例

### 示例 1: 开发新功能

```
用户: /dev 开发优惠券管理功能

AI: ### 需求澄清
我需要了解一些细节:
1. 优惠券的类型有哪些?
2. 需要哪些字段?
3. 是否需要前端页面?

用户: 满减券和折扣券,需要名称、金额、数量、有效期,需要前端页面

AI: ### 技术设计
涉及表: bus_coupon
激活技能: crud-development, database-ops, ui-pc

### 数据库设计
[生成建表SQL、字典SQL、菜单SQL]

### 后端开发
[生成 Entity、BO、VO、DAO、Service、Controller]

### 前端开发
[生成 API、页面组件]

### 测试验证
[执行测试]

### 文档更新
[更新项目状态]
```

### 示例 2: 快速生成CRUD

```
用户: crud bus_coupon

AI: ✅ 读取表结构: bus_coupon
✅ 生成 9 个文件
- Entity: Coupon.java
- BO: CouponBo.java
- VO: CouponVo.java
- DAO: ICouponDao.java / CouponDaoImpl.java
- Service: ICouponService.java / CouponServiceImpl.java
- Controller: CouponController.java
- Mapper: CouponMapper.java

💡 下一步:
- @code-reviewer 审查代码
- 启动服务测试
- 提交代码
```

### 示例 3: 代码审查

```
用户: @code-reviewer

AI: ## 🔍 代码审查报告

### 📊 审查概览
- 审查文件: 8 个
- 发现问题: 5 个
  - 严重: 1 个
  - 警告: 3 个
  - 建议: 1 个

### 🔴 严重问题
#### 1. UserServiceImpl.java:45
**问题**: Service 继承了 ServiceImpl
**修复**: ...
```

## 🎓 学习路径

### 初级
1. 阅读 `.claude/CLAUDE.md` 了解项目规范
2. 使用 `/dev` 命令完成第一个功能
3. 使用 `/check` 检查代码规范

### 中级
1. 学习 `.claude/skills/` 下的技能规范
2. 自定义斜杠命令
3. 使用代理进行代码审查和项目管理

### 高级
1. 修改钩子逻辑,定制工作流
2. 添加新的技能
3. 创建自定义代理

## 🔧 自定义配置

### 添加新技能

在 `.claude/skills/` 下创建新目录和 `SKILL.md`:

```bash
mkdir -p .claude/skills/my-skill
cat > .claude/skills/my-skill/SKILL.md << 'EOF'
# 我的技能

## 触发条件
- 关键词: xxx
- 场景: xxx

## 核心规范
...
EOF
```

然后在 `.claude/hooks/skill-forced-eval.js` 中添加技能名称。

### 添加新命令

在 `.claude/commands/` 下创建 `.md` 文件:

```bash
cat > .claude/commands/my-command.md << 'EOF'
# /my-command - 我的命令

## 描述
命令的简短描述

## 使用方法
```
/my-command <参数>
```

## 执行步骤
1. 步骤1
2. 步骤2
...
EOF
```

### 添加新代理

在 `.claude/agents/` 下创建目录和 `AGENT.md`:

```bash
mkdir -p .claude/agents/my-agent
cat > .claude/agents/my-agent/AGENT.md << 'EOF'
# My Agent - 我的代理

## 触发方式
- 手动调用 `@my-agent`

## 职责
代理的核心职责

## 工作流程
...
EOF
```

## ❓ 常见问题

### Q: Hooks 不触发?
A: 检查 `settings.json` 中是否正确配置了 hooks 路径,确保 Node.js 已安装。

### Q: 技能不激活?
A: 检查 `skill-forced-eval.js` 中的技能列表是否包含该技能。

### Q: 命令无法执行?
A: 确保命令文件是 `.md` 格式,路径正确。

### Q: 代理无响应?
A: 检查代理目录下是否有 `AGENT.md` 文件。

## 📚 参考资源

- [博客文章](https://blog.csdn.net/leoisaking/article/details/156203326)
- [视频教程](https://www.bilibili.com/video/BV1rnBKB2EME/)
- [Claude Code 官方文档](https://docs.anthropic.com/claude/code)

## 🤝 贡献

欢迎提交 Issue 和 Pull Request!

## 📄 许可证

MIT License

## 🙏 致谢

感谢原作者 [抓蛙师](https://blog.csdn.net/leoisaking) 分享的最佳实践!

---

**最后更新**: 2024-01-30
**维护者**: Your Name
**联系方式**: your.email@example.com
